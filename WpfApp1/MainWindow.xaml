<Window x:Class="VirtualDriveManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Virtual Drive Manager" Height="450" Width="700"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResizeWithGrip"
        Background="#1E1E1E">
    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   FontSize="24" 
                   FontWeight="Bold" 
                   Foreground="#0078D4"
                   Margin="0,0,360,15"><Run Language="en-gb" Text="Virtual Drive Manager"/></TextBlock>

		<!-- Control Buttons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Button x:Name="BtnMount" 
                    Content="🗔 Mount" 
                    Width="120" 
                    Height="35" 
                    Margin="0,0,10,0"
                    Background="#0078D4"
                    Foreground="White"
                    BorderThickness="0"
                    FontSize="14"
                    Click="BtnMount_Click"/>
            
            <Button x:Name="BtnUnmount" 
                    Content="✕ Unmount" 
                    Width="120" 
                    Height="35" 
                    Margin="0,0,10,0"
                    Background="#D32F2F"
                    Foreground="White"
                    BorderThickness="0"
                    FontSize="14"
                    Click="BtnUnmount_Click"/>
            
            <Button x:Name="BtnUnmountAll" 
                    Content="Unmount All" 
                    Width="120" 
                    Height="35" 
                    Margin="0,0,10,0"
                    Background="#424242"
                    Foreground="White"
                    BorderThickness="0"
                    FontSize="14"
                    Click="BtnUnmountAll_Click"/>
            
            <Button x:Name="BtnRefresh" 
                    Content="🔄 Refresh" 
                    Width="120" 
                    Height="35" 
                    Background="#424242"
                    Foreground="White"
                    BorderThickness="0"
                    FontSize="14"
                    Click="BtnRefresh_Click"/>
        </StackPanel>
        
        <!-- Mounted Drives List -->
        <ListView Grid.Row="2" 
                  x:Name="LvMountedDrives"
                  Background="#2D2D2D"
                  Foreground="White"
                  BorderBrush="#404040"
                  FontSize="13">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Drive" Width="80" DisplayMemberBinding="{Binding DriveLetter}"/>
                    <GridViewColumn Header="Image Path" Width="400" DisplayMemberBinding="{Binding ImagePath}"/>
                    <GridViewColumn Header="Type" Width="80" DisplayMemberBinding="{Binding Type}"/>
                    <GridViewColumn Header="Size" Width="100" DisplayMemberBinding="{Binding Size}"/>
                </GridView>
            </ListView.View>
        </ListView>
        
        <!-- Status Bar -->
        <StatusBar Grid.Row="3" 
                   Background="#2D2D2D" 
                   Foreground="#CCCCCC"
                   Margin="0,10,0,0">
            <StatusBarItem>
				<TextBlock x:Name="TxtStatus" Width="114" ><Run Language="en-gb" Text="Done"/></TextBlock>
			</StatusBarItem>
        </StatusBar>
		<TextBlock Grid.Row="0" 
			FontSize="16" 
			FontWeight="Bold" 
			Foreground="#FF026DB3"
			Margin="263,6,268,11" Height="30"><Run Language="en-gb" Text="by Adam Gal"/></TextBlock>
	</Grid>
</Window>